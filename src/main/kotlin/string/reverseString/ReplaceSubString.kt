package string.reverseString

/**
 *Replace Substring
 * Input: String: "I love Java" Replace "Java" with "Kotlin"
 * Output: "I love Kotlin"
 */

fun replaceString(str: String, oldStr: String, newStr: String): String{

    return str.replace(oldStr,newStr)

}